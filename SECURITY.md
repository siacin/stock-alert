# 安全说明

本软件面向个人电脑的只读行情与提醒，不承担交易执行职责。

## 不应上传的文件

`config.json`、`data/`、`.env`、API 密钥、Webhook 地址、Tailscale 账号与域名、数据库、日志、分析结果和虚拟环境均应留在本机。`.gitignore` 与发布检查只提供防误操作保护，不能撤回已经泄露的凭据。

发现真实密钥已经进入远端时，请先在对应服务商撤销/轮换密钥；仅删除当前文件并不能从 Git 历史中移除它。

## 访问边界

- 默认只允许本机管理；可选远程功能依赖 Tailscale Serve 身份与精确 Origin 校验。
- 远程端不开放 API 连接设置、推送地址和本地文件路径管理。
- 不要把服务直接暴露到公网，不要添加未经校验的反向代理，不要共享个人 Tailscale 账号。
- 本机管理员或恶意进程属于本软件信任边界之外；身份头校验不能抵御已控制本机的攻击者。
- 网页页面不应公开缓存。手机丢失时，请及时在 Tailscale 管理后台移除设备。

## 报告问题

报告时提供复现步骤、运行版本与已脱敏错误信息。不要在公开 Issue、讨论或截图中提交密钥、持仓配置、个人远程地址和完整日志。
